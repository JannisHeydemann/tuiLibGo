#pragma once

#include <string>
#include <vector>

namespace ctui {

    class mainMenu {
        private:
            std::string title;
            std::vector<std::string> options;
            // std::vector
    };

} // namespace ctui
