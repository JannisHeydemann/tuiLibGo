// Standalone test harness for developing ctui in isolation.
// This file is NOT part of libctui.a (the Makefile filters it out),
// so you can freely poke at the library here without affecting the
// Go build. Build & run with:  make run
//
// #include "mainMenu/mainMenu.hpp"

#include <iostream>

int main() {
    std::cout << "ctui test harness\n";

    // TODO: exercise your library here as you build it, e.g.
    // ctui::MainMenu menu;
    // menu.render();

    return 0;
}
